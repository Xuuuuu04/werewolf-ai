# 👤 人类玩家模式说明

## 🎮 两种人类玩家模式

本项目现在支持两种人类玩家参与方式：

### 1. 👤 单玩家模式（推荐）

**人类玩家只控制1个角色，其他8个角色由AI控制**

这是更符合真实狼人杀游戏体验的模式。你将作为9名玩家之一，与8个AI玩家一起游戏。

#### 特点
- ✅ 真实的游戏体验
- ✅ 可以观察AI之间的互动
- ✅ 更具挑战性
- ✅ 可以学习AI的策略

#### 配置文件

`configs/human_player.yaml`

```yaml
# 人类单玩家 + AI对战配置
env_config:
    n_player: 9
    n_role: 5
    n_werewolf: 3
    n_seer: 1
    n_guard: 1
    n_witch: 1
    n_villager: 3
    n_hunter: 0

# 人类玩家配置
human_player:
    enabled: true           # 是否启用人类玩家
    player_id: 5            # 人类控制的玩家编号（1-9）
                            # 留空或设为null则随机分配

# AI代理配置
agent_config:
    ai_model:
        model_type: qwen3-coder-plus
        model_params:
            tokenizer: null
            llm: qwen3-coder-plus
            temperature: 0.7
            base_url: https://apis.iflow.cn/v1/chat/completions
            api_key: your-api-key
```

#### 使用方式

**方式1：使用启动器**
```bash
python start_game.py
# 选择选项 3 - 人类单玩家模式
```

**方式2：直接运行**
```bash
python run_battle.py \
    --config configs/human_player.yaml \
    --log_save_path ./logs/human_single
```

#### 玩家编号分配

- **指定编号**：在配置文件中设置 `player_id: 5`（1-9之间）
- **随机分配**：设置 `player_id: null` 或留空

```yaml
# 示例1：固定为5号玩家
human_player:
    enabled: true
    player_id: 5

# 示例2：随机分配角色
human_player:
    enabled: true
    player_id: null
```

### 2. 👥 阵营模式（传统模式）

**人类玩家控制整个阵营（狼人或村民）**

这是原有的模式，人类控制某个阵营的所有玩家。

#### 特点
- 可以体验不同角色
- 需要切换多个角色视角
- 更适合测试和学习

#### 配置文件

`configs/human_vs_qwen.yaml`

```yaml
env_config:
    n_player: 9
    # ...

# 传统agent配置（按阵营分配）
agent_config:
    werewolf:
        model_type: human    # 人类控制所有狼人
        model_params:
            tokenizer: null
            llm: null
            temperature: 0.7
    villager:
        model_type: qwen3-coder-plus  # AI控制所有村民
        model_params:
            # ...
```

#### 使用方式

```bash
python start_game.py
# 选择选项 4 - 人类阵营模式
```

## 📝 配置详解

### 单玩家模式配置结构

```yaml
# 游戏环境配置
env_config:
    n_player: 9          # 玩家总数
    n_role: 5            # 角色种类
    n_werewolf: 3        # 狼人数量
    n_seer: 1            # 预言家数量
    n_guard: 1           # 守卫数量
    n_witch: 1           # 女巫数量
    n_villager: 3        # 平民数量
    n_hunter: 0          # 猎人数量

# 人类玩家配置
human_player:
    enabled: true        # 必须设为true
    player_id: 5         # 玩家编号（可选）
                         # 1-9: 固定编号
                         # null: 随机分配

# AI模型配置
agent_config:
    ai_model:            # 所有AI使用相同模型
        model_type: qwen3-coder-plus
        model_params:
            llm: qwen3-coder-plus
            temperature: 0.7
            base_url: https://your-api/v1/chat/completions
            api_key: your-key
```

### 关键参数说明

| 参数 | 说明 | 示例 |
|------|------|------|
| `human_player.enabled` | 是否启用单玩家模式 | `true` / `false` |
| `human_player.player_id` | 人类玩家编号 | `1-9` / `null` |
| `agent_config.ai_model` | AI模型配置 | 所有AI统一配置 |

## 🎯 游戏体验

### 单玩家模式游戏流程

1. **游戏开始**
   ```
   ══════════════════════════════════════════════════════════════════════
                            🎮 狼人杀游戏开始
   ══════════════════════════════════════════════════════════════════════
   
   ℹ️ 模式：单人类玩家 + AI
   ℹ️ 🎮 你将扮演 5 号玩家，身份是: 🐺 狼人
   ```

2. **轮到你行动时**
   ```
   ══════════════════════════════════════════════════════════════════════
                            💬 发言阶段
   ══════════════════════════════════════════════════════════════════════
   
   ──────────────────────────────────────────────────────────────────────
   │ 🎭 玩家编号: #5
   │ ℹ️ 你的身份: 🐺 狼人
   │ ✨ 当前阶段: 💬 发言阶段
   ──────────────────────────────────────────────────────────────────────
   ```

3. **其他玩家行动时**
   - 你会看到AI的发言和动作
   - 可以观察AI的策略
   - 学习狼人杀战术

### 策略建议

#### 作为好人角色
- 🔮 **预言家**：适时报出金水和查杀
- 🧪 **女巫**：注意是否有自刀
- 🛡️ **守卫**：保护关键神职
- 👤 **村民**：站边真预言家，归票推狼

#### 作为狼人角色
- 🐺 **悍跳**：冒充预言家
- 🗡️ **倒钩**：对队友假装怀疑
- 🎯 **刀神**：优先击杀神职
- 💬 **发言**：混淆视听，隐藏身份

## 🆚 两种模式对比

| 特性 | 单玩家模式 | 阵营模式 |
|------|-----------|---------|
| 控制角色数 | 1个 | 3-6个 |
| 游戏体验 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 真实性 | 高 | 低 |
| 难度 | 适中 | 较低 |
| 观察AI | ✅ 可以 | ❌ 较少 |
| 适合人群 | 所有玩家 | 测试/学习 |

## 🔧 高级配置

### 1. 随机角色分配

每次游戏随机分配身份给人类玩家：

```yaml
human_player:
    enabled: true
    player_id: null    # 或者完全不写这行
```

### 2. 固定角色体验

想体验特定身份，需要多次尝试或修改代码：

```yaml
human_player:
    enabled: true
    player_id: 5       # 固定编号，但身份仍然随机
```

### 3. 更换AI模型

使用不同的AI对手：

```yaml
agent_config:
    ai_model:
        model_type: gpt-4o    # 使用GPT-4o
        model_params:
            llm: gpt-4o
            temperature: 0.8   # 更高的创造性
```

## 📊 示例场景

### 场景1：新手体验

```yaml
# 配置：随机角色，温和AI
human_player:
    enabled: true
    player_id: null

agent_config:
    ai_model:
        model_params:
            temperature: 0.3   # 降低AI攻击性
```

### 场景2：高手挑战

```yaml
# 配置：固定位置，激进AI
human_player:
    enabled: true
    player_id: 1       # 首位发言，压力大

agent_config:
    ai_model:
        model_params:
            temperature: 0.9   # 提高AI创造性
```

### 场景3：学习观察

```yaml
# 配置：观察整局
human_player:
    enabled: true
    player_id: 9       # 最后发言，可观察所有人
```

## 🐛 故障排除

### 问题1：人类玩家没有生效

**检查**：
```yaml
human_player:
    enabled: true    # 确保设为true
```

### 问题2：player_id超出范围

**解决**：
- player_id必须在1-9之间
- 或设为null使用随机分配

### 问题3：AI不响应

**检查**：
- API密钥是否正确
- 网络连接是否正常
- API额度是否充足

## 💡 使用技巧

1. **首次游戏**：建议使用随机分配，体验不同角色
2. **学习阶段**：观察AI的发言和逻辑
3. **练习阶段**：固定编号，多次尝试同一位置的策略
4. **挑战阶段**：选择困难位置（如1号、2号）

## 🎓 进阶玩法

### 1. 多模型混合

修改代码支持不同AI使用不同模型（需自定义）

### 2. 难度调节

通过temperature参数调节AI"智力"：
- 0.1-0.3：保守，逻辑严谨
- 0.5-0.7：平衡，正常发挥
- 0.8-1.0：激进，创造性高

### 3. 录像回放

游戏日志保存在`--log_save_path`指定目录，可用于：
- 复盘学习
- 分析AI策略
- 分享精彩局

## 📝 总结

**单玩家模式是更真实、更有挑战性的游戏方式！**

- ✅ 只需控制1个角色
- ✅ 体验完整的9人局
- ✅ 观察AI互动和策略
- ✅ 提升个人游戏水平

**快来试试吧！**

```bash
python start_game.py
# 选择 3 - 人类单玩家模式
```

---

**祝游戏愉快！** 🎉

